version: 1

project:
  spec_dir: spec
  state_dir: .plaincode
  default_language: go

defaults:
  backend: cli:claude
  approval: patch
  retry_limit: 3

providers:
  cli:claude:
    kind: cli-claude
    binary: claude
  openai:gpt-4o:
    kind: openai
    model: gpt-4o
